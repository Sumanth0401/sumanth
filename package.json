{
  "name": "workflow",
  "version": "1.0.0",
  "private": true,
  "description": "WorkFlow — AI-Powered, Role-Based Employee Task & Accountability Platform (React + Express + Firebase + Gemini)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
